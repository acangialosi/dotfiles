# Claude configuration

- `settings.json` contains portable user settings.
- `settings.local.json` contains machine-specific overrides and is ignored by
  Git.
- `skills/` contains installable Claude Agent Skills.

Run `.\ai\install.ps1` from the repository root to install this configuration.
